define([
  'handlebars',
], function(
  Handlebars
) {
  'use strict';

  // methodで受け取って、整形後を介してやればよい
  // Handlebars.registerHelper('hoge', function(value) {
  //   return value + 'foobar';
  // });

});
